import { QueryProgressDirective } from './query-progress.directive';

/*describe('QueryProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryProgressDirective();
    expect(directive).toBeTruthy();
  });
});*/
