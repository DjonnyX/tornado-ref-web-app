import { NodeListFilterPipe } from './node-list-filter';

describe('NodeListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NodeListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
